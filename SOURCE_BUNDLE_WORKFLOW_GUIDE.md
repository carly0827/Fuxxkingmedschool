# 줄 + 야붙 + 전사문 → 최종본 생성 워크플로우 지시서

이 파일은 새로운 ChatGPT 세션에 업로드해서, 아래 워크플로우를 기준으로 같은 품질의 **최종본 PDF**를 만들도록 지시하기 위한 참조 문서이다. 이것은 모델을 영구적으로 학습시키는 파일이 아니라, **현재 대화에서 따라야 할 작업 규칙과 품질 기준을 전달하는 참조 파일**이다.

## 1) 입력 형식
사용자는 보통 아래 3가지를 함께 제공한다.

1. 파일명 끝이 `줄.pdf` 인 PDF
   - 원본 슬라이드에 사용자의 손필기/형광펜/메모가 들어 있다.
   - 이 파일의 기존 내용은 절대 손상되면 안 된다.
2. 파일명 끝이 `야붙.pdf` 또는 `야붙(n).pdf` 인 PDF
   - 줄 파일에는 없는 추가 페이지가 들어 있다.
   - 보통 아래 성격의 추가 페이지를 포함한다.
     - 맨 앞 설명 페이지 1장
     - 중간중간 기출 문제 페이지
     - 뒤쪽 `애매한 문제`, `모아보기` 페이지
   - 보통 1페이지 상단에 `야붙페이지: ...`, `모아보기 ...` 같은 힌트가 있다.
3. 전사문 텍스트
   - 사용자가 txt 파일로 주거나, 채팅에 그대로 붙여넣는다.
   - 페이지 번호/시간/파일명 표기가 섞여 있을 수 있다.

## 2) 목표
최종 목표는 다음을 모두 만족하는 **최종본 PDF**를 만드는 것이다.

- 줄 파일의 기존 필기/형광펜/표시를 **절대 손상하지 않음**
- 야붙 파일의 추가 페이지만 적절한 위치에 삽입
- 전사문 내용을 페이지별로 맞춰서 붙임
- 전사문이 길어도 잘리지 않게 **continuation 페이지를 무제한 허용**
- 전사문 누락 검토, 병합 검증, 페이지 계획 검토를 함께 수행

## 3) 줄/야붙 병합 규칙
병합 시 반드시 아래 규칙을 지킨다.

### 3-1. 줄 파일 보존 원칙
- 줄 파일 페이지는 **항상 원본 그대로 사용**한다.
- 야붙 페이지로 줄 페이지를 대체하면 안 된다.
- 글씨 깨짐 방지를 위해, 가능하면 PDF 페이지를 이미지로 재렌더링하지 말고 **원본 PDF 페이지 자체를 직접 복사**한다.
- 즉, 중간병합본은 가능한 한 **vector/text 보존 방식**으로 생성한다.

### 3-2. 야붙 추가 규칙
- 야붙 파일 1페이지는 항상 **최종 병합본 맨 앞**에 추가한다.
- 야붙 파일에만 있는 중간 추가 페이지는 **원래 그 자리에 삽입**한다.
- 야붙 뒤쪽의 `애매한 문제`, `모아보기` 페이지도 누락 없이 포함한다.
- 줄 파일의 순서와 페이지 내용은 손상 없이 유지한다.

### 3-3. 삽입 위치 결정
우선순위는 다음과 같다.

1. 야붙 1페이지에 적힌 명시적 힌트 사용
   - 예: `야붙페이지 13, 17, 34, 42`
   - 예: `모아보기 43~`
2. 명시적 힌트가 없으면 줄/야붙의 공통 페이지를 순서대로 비교하여
   - 제목
   - 부제목
   - 상단 bullet
   - 핵심 용어
   - 전체 페이지 텍스트
   를 이용해 **단조(monotonic) 정렬**로 삽입 페이지를 찾는다.

## 4) 전사문 정렬 규칙
전사문은 슬라이드와 완전히 같은 언어가 아닐 수 있다. 특히 **강의록은 영어, 교수 설명은 한국어**인 경우가 있으므로 아래 규칙을 꼭 사용한다.

### 4-1. 핵심어 비교 방식
페이지 매칭 시 아래 정보를 함께 본다.

- 페이지 제목
- 페이지 부제목
- 상단 짧은 핵심 문장
- 본문 핵심 bullet
- 그림 옆 라벨 핵심어
- 전사문 핵심어

### 4-2. 의학용어 정규화
교수 설명이 한국어여도, 핵심 단어를 가능한 한 **영문 의학용어 canonical form**으로 바꿔서 비교한다.

예시:
- 간문맥 → portal vein
- 총담관 → common bile duct
- 담낭 → gallbladder
- 위문/문지기 → pylorus
- 큰그물막 → greater omentum
- 작은그물막 → lesser omentum
- 복막 → peritoneum
- 배안/복강 → abdominal cavity

즉, **한국어 설명 ↔ 영어 슬라이드** 비교가 가능하도록 의학용어 alias 사전을 사용한다.

### 4-3. 페이지 제목/부제목 우선
전사문 핵심을 뽑을 때는 무조건 슬라이드 전체를 한번 훑고,

1. 페이지 제목
2. 페이지 부제목
3. 상단 핵심 bullet
4. 본문 핵심어

순으로 가중치를 두고 정렬한다.

### 4-4. 전사문 배치 원칙
- 전사문 각 블록은 가장 잘 맞는 페이지에 붙인다.
- 애매하면 **직전 페이지**에 붙이는 것을 우선 고려한다.
- 정렬은 전체적으로 앞으로만 진행하는 **monotonic alignment**를 사용한다.
- 전사문 원문은 요약이 아니라 **원문 자체를 유지**하는 것을 우선한다.

## 5) continuation 페이지 규칙
- 전사문이 길어서 한 페이지 아래에 다 안 들어가면 **계속 다음 continuation 페이지를 추가**한다.
- 2페이지 제한을 두지 않는다.
- 목표는 **전사문 전체가 잘리지 않고 모두 실리는 것**이다.
- continuation 페이지가 여러 장 생겨도 괜찮다.

## 6) 최종본 생성 시 절대 지켜야 할 품질 기준
1. 줄 파일의 필기/형광펜/손글씨가 사라지면 안 된다.
2. 중간병합본부터 글씨가 깨지면 안 된다.
3. 야붙의 추가 페이지가 빠지면 안 된다.
4. 전사문이 일부라도 잘리면 안 된다.
5. 전사문 페이지 배정은 제목/부제목/의학용어 정규화를 사용해 최대한 정확히 해야 한다.
6. 검토 파일 없이 PDF만 내지 말고, 검증 결과도 함께 만들어야 한다.

## 7) 검증 단계
최종본 생성 후 아래 검증을 반드시 수행한다.

### 7-1. 병합 검증
- 줄 파일의 모든 페이지가 **정확히 1번씩 유지**되었는지
- 야붙 1페이지가 맨 앞에 들어갔는지
- 중간 삽입 페이지가 모두 들어갔는지
- 모아보기 페이지가 모두 들어갔는지

### 7-2. 페이지 계획 저장
- 어떤 최종 페이지가
  - 줄 원본인지
  - 야붙 삽입 페이지인지
  - continuation 페이지인지
  기록한 `페이지계획.json`을 만든다.

### 7-3. 전사문 누락 검토
- 전사문 각 블록이 어느 페이지에 배정됐는지 기록
- 배정 confidence를 `high / medium / low`로 저장
- 낮은 confidence 블록 따로 표시
- 전사문 전체와 최종 PDF에 실린 전사문 전체를 정규화 비교
- 누락/순서 꼬임 의심 여부를 보고서로 저장

## 8) 출력 파일명 규칙
입력 줄 파일명이 예를 들어

`어쩌고_줄.pdf`

이면 출력은 보통 아래처럼 만든다.

- `어쩌고_최종본.pdf`
- `어쩌고_줄과야붙_중간병합본.pdf`
- `어쩌고_최종본_페이지계획.json`
- `어쩌고_최종본_줄야붙병합검증.json`
- `어쩌고_최종본_전사문누락검토.json`
- `어쩌고_최종본_전사문누락검토.txt`
- `어쩌고_최종본.zip`
- `ascii_safe_name(어쩌고_최종본)_safe_download.zip`
- `ascii_safe_name(어쩌고_최종본)_safe_full_archive.zip`

## 9) 새 ChatGPT에게 바로 줄 수 있는 실행 지시 문구
아래 문구를 함께 주면 된다.

---
이 번들 안의 `CHATGPT_학습용_최종본_워크플로우_지시서.md`와 `code/` 폴더의 파이썬 파일을 기준으로 작업해줘.

내가 주는 입력은 `줄` 파일, `야붙` 파일, 전사문 텍스트야.

반드시 아래를 지켜줘:
- 줄 파일의 기존 필기 내용 절대 손상 금지
- 야붙의 추가 페이지만 삽입
- 야붙 1페이지는 맨 앞에 추가
- 전사문은 제목/부제목/핵심 용어/영문 의학용어 정규화 비교로 페이지 매칭
- 전사문이 길어도 continuation 페이지로 끝까지 모두 실어
- 중간병합본부터 글씨 깨짐 금지
- 전사문 누락 검토, 페이지 계획, 병합 검증 포함
- 최종 출력 파일명은 줄 파일명을 기준으로 `최종본`으로 바꿔서 제공
---

## 10) 참고
이 지시서는 현재 사용자가 확정한 작업 규칙을 문서화한 것이다. 새로운 세션의 ChatGPT는 이 파일을 읽고 같은 워크플로우를 최대한 재현해야 한다.


## 8) 추가 반영된 최신 규칙
- 전체 전사문 정리표는 DOCX를 먼저 만든 뒤 PDF로 변환한다. 변환 실패 시에만 이미지 기반 PDF를 보조적으로 사용한다.
- 전체 전사문 정리표의 DOCX와 생성정보 JSON도 함께 보존한다.
- 사용자가 최종본을 요청하면 직접 PDF보다 `*_safe_download.zip`을 우선 전달하는 방식을 권장한다.
- 전체 전사문 정리본은 강의록 원본 슬라이드 -> 시험용 표형 정리본 관계를 모사하도록, 큰 섹션 단위로 묶고 슬라이드별 핵심을 2열 표 블록으로 압축해 생성한다.
- 전체 정리본의 각 블록은 시간/페이지를 나열하지 않고, `항목 | 정리` 형식의 간결한 표로 표현한다.
- 오른쪽 하단 요약 칸도 같은 원칙을 따르며, 전사문 구어체를 그대로 복붙하지 않고 개념/대상/특징 중심의 간결한 표로 바꿔 넣는다.
- 파란/빨간/보라 글씨 규칙은 그대로 유지한다.
- 다운로드 오류를 줄이기 위해 배포 번들 내부 파일명은 ASCII로 유지하고, 실행 결과도 한국어 원본 zip과 별도로 `*_safe_download.zip`을 같이 만든다.


## 11) 전달 전 링크/열기 검증 단계
최종 산출물을 사용자에게 전달하기 직전에 반드시 아래를 확인한다.

1. 실제로 링크를 걸 파일이 존재하는지 확인
2. 파일 크기가 0이 아닌지 확인
3. PDF는 열 수 있고 페이지 수가 1 이상인지 확인
4. ZIP은 열 수 있고 무결성 검사(testzip)에 실패하지 않는지 확인
5. DOCX는 zip 구조(word/document.xml)가 정상인지 확인
6. 직접 링크는 가능하면 한글/공백 원본 파일명 대신 **ASCII direct safe copy**를 사용
7. direct safe copy가 검증 실패하면 원본 PDF 직접 링크를 보내지 말고 `*_safe_download.zip`만 우선 전달

이 단계의 결과로 아래 파일을 함께 만든다.
- `*_delivery_link_check.json`
- `*_delivery_link_check.txt`

응답 시에는 위 검증 파일에서 `passed=true`인 direct safe copy만 링크로 제시한다. `%20`이 들어간 경로나 손으로 바꾼 URL 인코딩 문자열을 임의로 쓰지 않는다.
